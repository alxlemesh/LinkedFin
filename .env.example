NODE_ENV=development

# ── LinkedFin database settings ──────────────────────────────────────────────
# Set DB_SOCKET to a Unix socket path (e.g. /tmp/mysql.sock) OR
# set DB_HOST + DB_PORT for a TCP connection.
DB_HOST=localhost
DB_PORT=3306
DB_SOCKET=
DB_USER=root
DB_PASS=
DB_NAME=linkedfin
